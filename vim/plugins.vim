" Plugin declarations shared between plain vim (vim/vim.vim, via vim-plug)
" and IdeaVim (.ideavimrc, via IdeaVim's own Plug parser) -- both read the
" same `Plug 'author/repo'` syntax. Neovim doesn't source this: it has
" built-in equivalents for both of these, see nvim/init.lua.
Plug 'machakann/vim-highlightedyank' " highlight copied text
Plug 'tpope/vim-commentary'          " gcc/gc commenting motions
